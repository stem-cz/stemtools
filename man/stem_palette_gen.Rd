% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color-palettes.R
\name{stem_palette_gen}
\alias{stem_palette_gen}
\title{Colour palette generator factory}
\usage{
stem_palette_gen(palette = "modern", direction = 1)
}
\arguments{
\item{palette}{Name of the palette. See \code{\link[=stem_palette]{stem_palette()}} for the full list of valid names.}

\item{direction}{\code{1} (default) returns colours in their natural order; \code{-1} reverses the order.}
}
\value{
A \verb{function(n)} that returns a character vector of \code{n} hex colour codes.
}
\description{
Returns a function \verb{function(n)} that selects \code{n} colours from the named palette.
This factory is used internally by \code{\link[=scale_colour_stem]{scale_colour_stem()}} and \code{\link[=scale_fill_stem]{scale_fill_stem()}} to
satisfy ggplot2's discrete scale interface, but can also be called directly.

Diverging palettes use \code{\link[=select_diverging]{select_diverging()}} to pick colours that preserve the
symmetric structure of the palette. All other palette types return the first \code{n} colours,
optionally reversed.
}
\examples{
# Create a generator then call it for 3 colours
gen <- stem_palette_gen("modern")
gen(3)

# Reversed order
stem_palette_gen("div1", direction = -1)(5)
}
\seealso{
\code{\link[=stem_palette]{stem_palette()}}, \code{\link[=select_diverging]{select_diverging()}}, \code{\link[=scale_colour_stem]{scale_colour_stem()}}
}
