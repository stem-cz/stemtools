% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/officer.R
\name{stem_add_chart}
\alias{stem_add_chart}
\title{Add a Stem plot to a PowerPoint or Word document as a native chart}
\usage{
stem_add_chart(
  x,
  plot,
  width = NULL,
  height = NULL,
  left = NULL,
  top = NULL,
  units = "cm",
  location = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{\link[officer:read_pptx]{officer::read_pptx()}} or \code{\link[officer:read_docx]{officer::read_docx()}} document.}

\item{plot}{A ggplot2 object created by a Stem plotting function, or an
\code{ms_barchart} already produced by \code{\link[=stem_as_mschart]{stem_as_mschart()}}.}

\item{width, height}{Size of the chart, in \code{units}. In Word both default to a
readable 15 x 10 cm. In PowerPoint both default to \code{NULL}, which uses the
body placeholder of the slide layout; give them to size the chart by hand.}

\item{left, top}{PowerPoint only: position of the chart's top-left corner, in
\code{units}. Only used when \code{width} and \code{height} are given; both default to
\code{0}.}

\item{units}{Unit of \code{width}, \code{height}, \code{left} and \code{top}: \code{"cm"} (default),
\code{"mm"} or \code{"in"}.}

\item{location}{PowerPoint only: an \code{\link[officer:ph_location]{officer::ph_location()}} giving where to
place the chart. Takes precedence over \code{width}/\code{height}/\code{left}/\code{top}.}

\item{...}{Passed to \code{\link[=stem_as_mschart]{stem_as_mschart()}}.}
}
\value{
The document \code{x}, updated.
}
\description{
Converts \code{plot} with \code{\link[=stem_as_mschart]{stem_as_mschart()}} and adds it to an existing officer
document. For a PowerPoint document the chart is placed on the current
slide, so add a slide with \code{\link[officer:add_slide]{officer::add_slide()}} first.
}
\examples{
\dontrun{
doc <- officer::read_pptx()
doc <- officer::add_slide(doc, "Title and Content", "Office Theme")
doc <- stem_add_chart(doc, stem_barplot(trust, government))
print(doc, target = "chart.pptx")
}
}
\seealso{
\code{\link[=stem_as_mschart]{stem_as_mschart()}}
}
