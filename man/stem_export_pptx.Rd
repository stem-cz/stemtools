% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/officer.R
\name{stem_export_pptx}
\alias{stem_export_pptx}
\title{Export Stem plots to PowerPoint as native charts}
\usage{
stem_export_pptx(
  ...,
  path,
  template = NULL,
  layout = "Title and Content",
  master = "Office Theme",
  width = NULL,
  height = NULL,
  left = NULL,
  top = NULL,
  units = "cm",
  location = NULL,
  chart_args = list()
)
}
\arguments{
\item{...}{One or more ggplot2 objects created by the Stem plotting
functions (or \code{ms_barchart} objects from \code{\link[=stem_as_mschart]{stem_as_mschart()}}). A single
list of plots is also accepted.}

\item{path}{Path of the \code{.pptx} file to write.}

\item{template}{Optional path to a \code{.pptx} template to build on. Defaults to
officer's blank presentation.}

\item{layout, master}{Name of the slide layout and of the master to use for
the new slides.}

\item{width, height}{Size of each chart, in \code{units}. Both default to \code{NULL},
which uses the body placeholder of \code{layout}, so a template's own geometry
is respected. Give them to size the charts by hand.}

\item{left, top}{Position of each chart's top-left corner, in \code{units}. Only
used when \code{width} and \code{height} are given; both default to \code{0}.}

\item{units}{Unit of \code{width}, \code{height}, \code{left} and \code{top}: \code{"cm"} (default),
\code{"mm"} or \code{"in"}.}

\item{location}{An \code{\link[officer:ph_location]{officer::ph_location()}} giving where to place the charts.
Takes precedence over \code{width}/\code{height}/\code{left}/\code{top}.}

\item{chart_args}{A named list of arguments passed on to
\code{\link[=stem_as_mschart]{stem_as_mschart()}}.}
}
\value{
The path, invisibly.
}
\description{
Writes one slide per plot, each holding a native Office chart whose source
data can be edited in Excel.
}
\examples{
\dontrun{
stem_export_pptx(
  stem_barplot(trust, government, title_show = TRUE),
  stem_inline(trust, police),
  path = "trust.pptx"
)

# A 24 x 12 cm chart, 2 cm from the left and 4 cm from the top of the slide.
stem_export_pptx(
  stem_barplot(trust, government),
  path = "trust.pptx",
  width = 24, height = 12, left = 2, top = 4
)
}
}
\seealso{
\code{\link[=stem_as_mschart]{stem_as_mschart()}}, \code{\link[=stem_export_docx]{stem_export_docx()}}
}
