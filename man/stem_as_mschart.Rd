% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/officer.R
\name{stem_as_mschart}
\alias{stem_as_mschart}
\title{Convert a Stem plot into a native Microsoft chart}
\usage{
stem_as_mschart(
  plot,
  title = NULL,
  labels = NULL,
  legend = NULL,
  num_fmt = NULL,
  axis_num_fmt = "0\\"\%\\"",
  axis_show = TRUE,
  axis_major_unit = NULL,
  gap_width = 30,
  value_name = "\%"
)
}
\arguments{
\item{plot}{A ggplot2 object created by \code{\link[=stem_barplot]{stem_barplot()}}, \code{\link[=stem_inline]{stem_inline()}},
\code{\link[=stem_battery]{stem_battery()}} or \code{\link[=stem_multiselect]{stem_multiselect()}}.}

\item{title}{Chart title. Defaults to the ggplot title (set with
\code{title_show = TRUE}), or no title when the plot has none. Use \code{NA} to drop
the title.}

\item{labels}{If \code{TRUE}, prints a data label on every segment. Defaults to
whatever the ggplot does.}

\item{legend}{If \code{TRUE}, shows a legend. Defaults to \code{TRUE} for plots with a
fill variable and \code{FALSE} otherwise, matching the ggplot.}

\item{num_fmt}{Excel number format code for the data labels, e.g. \code{"0"},
\code{"0.0"} or \verb{"0 \\"\%\\""}. By default the chart prints the plot's own label
text instead of formatting the worksheet values, so the accuracy
(\code{label_accuracy}) and the blanks left by \code{label_hide} carry over exactly.
Give a format to label the values from the worksheet instead, which keeps
the labels live when the data are edited in Excel.}

\item{axis_num_fmt}{Excel number format code for the value axis. Defaults to
\verb{"0\\"\%\\""}, matching the percentage axis of the Stem plots.}

\item{axis_show}{If \code{TRUE} (default), draws the value axis.}

\item{axis_major_unit}{Spacing of the value axis ticks, in percentage
points. Defaults to \code{25} for stacked charts and \code{10} otherwise.}

\item{gap_width}{Gap between bars, as a percentage of the bar width.}

\item{value_name}{Name of the value column in the embedded worksheet; it is
also the series name of charts without a fill variable.}
}
\value{
An object of class \code{ms_barchart}, ready for
\code{\link[officer:ph_with]{officer::ph_with()}} / \code{\link[mschart:body_add_chart]{mschart::body_add_chart()}} or for the Stem helpers
\code{\link[=stem_add_chart]{stem_add_chart()}}, \code{\link[=stem_export_pptx]{stem_export_pptx()}} and \code{\link[=stem_export_docx]{stem_export_docx()}}.
}
\description{
Takes a ggplot2 object created by one of the Stem plotting functions and
returns an \code{\link[mschart:ms_barchart]{mschart::ms_barchart()}} that renders as closely as possible to
the ggplot original, but is a \emph{native} Office chart: once placed in a
PowerPoint or Word file it carries its own embedded worksheet, so the source
data can be opened and edited in Excel.
}
\details{
The aggregated data, the category order, the palette, the label formatting
and the fonts are all read back off the ggplot object, so the native chart
needs no recomputation and stays in sync with the ggplot version.

A few ggplot features have no Office equivalent and are dropped, with a
message: confidence interval error bars (\code{errorbar = TRUE}) and the
background bars of \code{\link[=stem_multiselect]{stem_multiselect()}}.
}
\examples{
\dontrun{
chart <- stem_as_mschart(stem_barplot(trust, government))

stem_export_pptx(
  stem_barplot(trust, government),
  stem_battery(trust, items = c(police, eu, government, army)),
  path = "charts.pptx"
)
}
}
\seealso{
\code{\link[=stem_add_chart]{stem_add_chart()}}, \code{\link[=stem_export_pptx]{stem_export_pptx()}}, \code{\link[=stem_export_docx]{stem_export_docx()}}
}
