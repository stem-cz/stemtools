% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{stem_battery}
\alias{stem_battery}
\title{Stacked bar plot for a battery of like items}
\usage{
stem_battery(
  data,
  items,
  weight = NULL,
  order_by = NULL,
  item_label = TRUE,
  palette = "div1",
  direction = 1,
  labels = TRUE,
  label_accuracy = 1,
  label_suffix = "",
  label_hide = 0.05,
  label_color = "black",
  label_bicolor = TRUE
)
}
\arguments{
\item{data}{Data frame holding the item (and group) variables.}

\item{items}{Items to plot. Supports tidyselect helpers (e.g.
\code{dplyr::starts_with()}).}

\item{weight}{Optional survey weights.}

\item{order_by}{Optional vector of response categories used to order the
items, e.g. \code{c("Definitely Agree", "Rather Agree")} orders items by their
combined share of those two categories.}

\item{item_label}{If \code{TRUE} (default), item labels (the \code{"label"} attribute)
are used instead of variable names. Falls back to names, with a warning, if
any item lacks a label.}

\item{palette}{Name of a Stem palette. See \code{\link[=stem_palette]{stem_palette()}}.}

\item{direction}{Palette direction. Use \code{-1} to reverse the colours.}

\item{labels}{If \code{TRUE}, prints a percentage label at the end of each bar.}

\item{label_accuracy}{Rounding accuracy of labels. \code{1} gives whole numbers,
\code{0.1} one decimal place.}

\item{label_suffix}{Suffix appended to labels. Defaults to \code{""}.}

\item{label_hide}{Proportions below this threshold are left unlabelled.}

\item{label_color}{Colour of the segment labels. Defaults to \code{"black"}.}

\item{label_bicolor}{If \code{TRUE} (default), the labels of the two side
(extreme) response categories are drawn in white, while all other labels
use \code{label_color}. Set to \code{FALSE} to colour every label with \code{label_color}.}
}
\value{
A ggplot2 object.
}
\description{
Plots a set of categorical variables that share the same response categories
(e.g. a battery of Likert items) as a single chart, drawing one stacked
horizontal bar per item. Internally the items are reshaped to long format and
handed to the same machinery as \code{\link[=stem_barstack]{stem_barstack()}}, with the item taking the
role of the group.
}
\examples{
\dontrun{
stem_battery(trust, items = c(police, eu, government, army))
stem_battery(trust,
             items = c(police, eu, government, army),
             weight = W,
             order_by = c("Definitely Agree", "Rather Agree"))
}
}
