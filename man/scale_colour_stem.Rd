% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color-palettes.R
\name{scale_fill_stem}
\alias{scale_fill_stem}
\alias{scale_colour_stem}
\alias{scale_color_stem}
\title{stem discrete colour and fill scales for ggplot2}
\usage{
scale_fill_stem(palette = "modern", direction = 1, ...)

scale_colour_stem(palette = "modern", direction = 1, ...)

scale_color_stem(palette = "modern", direction = 1, ...)
}
\arguments{
\item{palette}{Name of the palette. See \code{\link[=stem_palette]{stem_palette()}} for the full list of valid names.
Defaults to \code{"modern"}.}

\item{direction}{\code{1} (default) uses colours in their natural order; \code{-1} reverses the order.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}}.}
}
\value{
A ggplot2 \code{Scale} object to be added to a plot with \code{+}.
}
\description{
Discrete ggplot2 scales that apply a stem colour palette to the \code{colour} or \code{fill} aesthetic.
Three aliases are provided:
\itemize{
\item \code{scale_colour_stem()} — maps to the \code{colour} aesthetic (British spelling).
\item \code{scale_color_stem()} — alias for \code{scale_colour_stem()} (American spelling).
\item \code{scale_fill_stem()} — maps to the \code{fill} aesthetic.
}

All functions delegate to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}} via \code{\link[=stem_palette_gen]{stem_palette_gen()}}, so any argument
accepted by \code{discrete_scale()} (e.g. \code{name}, \code{breaks}, \code{labels}, \code{na.value}, \code{drop}) can
be passed through \code{...}.
}
\examples{
ggplot2::ggplot(data = mtcars,
                mapping = ggplot2::aes(x = mpg, y = hp, color = as.factor(am))) +
  ggplot2::geom_point() +
  scale_colour_stem()
}
\seealso{
\code{\link[=stem_palette]{stem_palette()}}, \code{\link[=stem_palette_gen]{stem_palette_gen()}}, \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}}
}
