% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/officer.R
\name{stem_num_fmt}
\alias{stem_num_fmt}
\title{Derive an Excel number format from Stem percentage labels}
\usage{
stem_num_fmt(labels, fallback = "0")
}
\arguments{
\item{labels}{Character vector of formatted labels (the \code{stem_label} column).}

\item{fallback}{Number format used when no label can be parsed.}
}
\value{
A length-one character vector holding an Excel number format code.
}
\description{
Internal helper. Reads the preformatted \code{stem_label} column back to work out
how many decimal places and which suffix the ggplot labels use, and returns
the matching Excel number format code. Values handed to the chart are
percentages (0-100), so the \verb{\%} sign is escaped rather than applied as a
format multiplier.
}
\keyword{internal}
