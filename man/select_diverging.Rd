% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color-palettes.R
\name{select_diverging}
\alias{select_diverging}
\title{Select n colours from a diverging palette}
\usage{
select_diverging(palette, n)
}
\arguments{
\item{palette}{Character vector of hex colour codes representing the full diverging palette.}

\item{n}{Number of colours to select. Must be between 1 and \code{length(palette)}.}
}
\value{
Character vector of \code{n} hex colour codes in original palette order.
}
\description{
Selects \code{n} colours from a diverging palette while preserving its symmetric, center-anchored
structure. Rather than simply taking the first \code{n} colours, this function samples
symmetrically from both ends of the palette:
\itemize{
\item \strong{Odd \code{n}}: the palette midpoint is always included, with an equal number of colours
drawn from each side.
\item \strong{Even \code{n}}: colours are drawn evenly from the left and right halves.
}

The selected colours are always returned in their original palette order.
}
\examples{
pal <- stem_palette("modern")

# Odd n: midpoint included, balanced sides
select_diverging(pal, n = 3)

# Even n: two from each end
select_diverging(pal, n = 4)
}
\seealso{
\code{\link[=stem_palette_gen]{stem_palette_gen()}}, \code{\link[=stem_palette]{stem_palette()}}
}
