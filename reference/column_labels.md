# Extract variable labels

Returns the `"label"` attribute of each column, or `NA` when it is
missing.

## Usage

``` r
column_labels(data)
```

## Arguments

- data:

  A data frame.

## Value

A named character vector of labels, one per column.
